## Packages
framer-motion | Smooth layout transitions and micro-interactions
lucide-react | Beautiful icons for the dashboard

## Notes
Authentication uses JWT stored in localStorage
API requests to /api/tasks must include Authorization: Bearer <token>
User roles: 'admin' | 'user'
